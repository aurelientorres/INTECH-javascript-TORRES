const string = "bonjour kevin barao";

const search = "kevin";

function contains(haystack, needle, startIndex){

    if(startIndex === undefined) startIndex = 0;

    for(let i = startIndex; i < string.length(); i++){

    }

    return -1;
}


console.log(contains(string, search, 2))


console.log(contains(string, search))